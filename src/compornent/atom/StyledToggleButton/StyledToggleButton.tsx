import React from 'react'
import { css } from '@emotion/react'

const StyledToggleButton = (children: React.ReactNode) => {
  return <div css={css``}>{children}</div>
}

export default StyledToggleButton
